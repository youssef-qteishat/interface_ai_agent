**/__pycache__/
*.pyc

**/.pytest_cache/

# Real secrets: never committed.
.env
.env.*
# ...but the placeholder template IS committed, so a fresh clone knows what to set.
!.env.example

.DS_Store

# Model-free smoke-test output: a development aid, not the committed deliverable.
evidence/drive/

# The scripted handoff rehearsal. Regenerate it any time for free; the committed
# artifact is the real capture, not the practice run.
evidence/handoff-fake

evidence/run_*/

evidence/handoff-demo/

evidence/replay-dialog/

evidence/replay-member-not-found/

evidence/replay-recovery/

evidence/replay-success/

docker-compose.override.yml

*.log
